enum AppBarHeaders { home, aboutMe, projects, certifcates ,contact }

enum AppBarHeadersAxis { horizontal, vertical }

enum DeviceType {
  mobile,
  ipad,
  smallScreenLaptop,
  largeScreenDesktop,
  extraLargeTV
}
